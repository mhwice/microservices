apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments-mongo # this is what the deployment looks for
  template:
    metadata:
      labels:
        app: payments-mongo # label applied to the pod
    spec:
      containers:
        - name: payments-mongo
          image: mongo # official mongo image
---
apiVersion: v1
kind: Service # default service type is ClusterIP
metadata:
  name: payments-mongo-srv
spec:
  selector:
    app: payments-mongo # tells service which pods to govern access to
  ports:
    - name: db # this name is just for logging purposes
      protocol: TCP
      port: 27017 # default port for the official mongo image
      targetPort: 27017
